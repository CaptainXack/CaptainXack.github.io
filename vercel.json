{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "rm -rf dist && mkdir dist && cp coupon-hunt-hq/index-fixed.html dist/index.html && cp coupon-hunt-hq/manifest.webmanifest dist/manifest.webmanifest && cp coupon-hunt-hq/sw.js dist/sw.js",
  "outputDirectory": "dist",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "Cache-Control", "value": "public, max-age=0, must-revalidate" }
      ]
    }
  ]
}
